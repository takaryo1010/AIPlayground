# Notification Settings
# The server will use these settings to send error alerts.
#
# Notification Priority:
# 1. If DISCORD_WEBHOOK_URL is set, alerts will be sent to Discord.
# 2. If DISCORD_WEBHOOK_URL is NOT set, the server will fall back to using email.
#    (Requires SMTP_HOST, SMTP_PORT, SMTP_USER, SMTP_PASS to be set).

# --- Discord (Primary) ---
DISCORD_WEBHOOK_URL=your_discord_webhook_url

# --- Email (Fallback) ---
# For Gmail, you might need to use an "App Password" instead of your regular password.
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your.email@example.com
SMTP_PASS=your_password_or_app_password
